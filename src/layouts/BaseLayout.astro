---
import "../styles/global.css";

const {
  title = "Goddamn Apps",
  description = "狗日的 App 档案：记录那些坑用户、偷隐私、玩暗黑模式、滥用权限的产品和公司。"
} = Astro.props;

const navItems = [
  { href: "/", label: "案例" },
  { href: "/tags/", label: "标签" },
  { href: "/about/", label: "关于" }
];
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={Astro.url} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <title>{title}</title>
  </head>
  <body>
    <div class="site-shell">
      <header class="site-header">
        <div class="header-inner">
          <a class="brand" href="/" aria-label="Goddamn Apps 首页">
            <img src="/favicon.svg" alt="" width="34" height="34" />
            <span>Goddamn Apps</span>
          </a>
          <nav class="nav" aria-label="主导航">
            {navItems.map((item) => <a href={item.href}>{item.label}</a>)}
          </nav>
        </div>
      </header>
      <main class="main">
        <slot />
      </main>
      <footer class="footer">
        <div class="footer-inner">
          <span>狗日的 App 档案。只收有证据的用户伤害记录。</span>
          <a href="mailto:tips@goddamnapps.com">tips@goddamnapps.com</a>
        </div>
      </footer>
    </div>
  </body>
</html>
